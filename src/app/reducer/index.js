import initState from "../../common/initState";
// import * as types from "../../common/ActionTypes";

const _initState = { ...initState, ...{} };

export default (state = _initState, /* actions */) => {
  return state;
};
